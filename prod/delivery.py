import os
import json

script_dir = os.path.dirname(os.path.abspath(__file__))
output_dir = os.path.join(script_dir, '..', 'outputs')
reports_dir = os.path.join(script_dir, '..', 'reports')






# create HTML content from the json reports

# convert or create downloadable PDF

# send findings from audit and vulerability report to SNOW or JIRA (whcihever is easy)

# package and send generated code to storage


# clean up files?